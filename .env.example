# Docker Compose — local development defaults (copy to `.env` at repo root).
POSTGRES_USER=wish
POSTGRES_PASSWORD=wish
POSTGRES_DB=wish
# Host port for external DB access (container always listens on 5432)
POSTGRES_HOST_PORT=15432
